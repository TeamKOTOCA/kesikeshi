# kesikeshi
オンデバイスで画像の背景削除を行うサイト

# なんかのモデル落としてきて使おうかなと。
# PWA対応

- `manifest.webmanifest` を追加して、ホーム画面ピン留め用のアイコンとテーマカラーを定義しました。
- `pwa-sw.js` でアプリシェルをキャッシュして、再訪問時も `index.html`/`style.css`/`worker.js` を高速に返します。
- `index.html` からサービスワーカーを登録しているので、一度読み込めば自動でオフライン対応が有効になります。
